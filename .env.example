# Optional: enables live Gemini answers/quizzes. Without it, Orbit uses curated offline demos.
VITE_GEMINI_API_KEY=

# Phase 0 — add when ready (auth switches from local demo → Supabase)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
